```javascript
const obj = {
    show: true,
    users: [
        {
            name: '小明',
            url: 'url1'
        },
        {
            name: '小红',
            url: 'url2'
        }
    ]
}

const str = `
<ul>
  <% if (obj.show) { %>
    <% for (var i = 0; i < obj.users.length; i++) { %>
      <li>
        <a href="<%= obj.users[i].url %>">
          <%= obj.users[i].name %>
        </a>
      </li>
    <% } %>
  <% } else { %>
    <p>不展示列表</p>
  <% } %>
</ul>
`

const str2 = `
<ul>
    <li>1</li>
    <li>2</li>
    <li>3</li>
    <li>4</li>
</ul>
`

const str3 = `
<div>
    <div>
        <p>hello</p>
    </div>
    <div>
        <p>world</p>
    </div>
    ！！
</div>
`
```

```javascript
// AST 树（抽象语法树）
const root = {
    tagName: 'div',
    nodeType:  1 // 1 - 元素； 2 - 属性； 3 - 文本
    children: [
        {
            nodeType: 1,
            tagName: 'p',
            children: [
                {
                    nodeType: 3,
                    nodeValue: 'hello world'
                },
            ]
        }
    ]
}
```

1. 找到配对的标签组

```javascript
function createDomByTemplate(str) {

}



```